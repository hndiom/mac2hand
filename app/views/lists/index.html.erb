<%= render :partial => "common/page_header" %>
<div class="container">
  <div class="row">
    <div class="span12 toolbar" align="center">
      <%= link_to("我要買二手", new_want_path, :class => "btn btn-info btn-large") %>
      <%= link_to("我要賣二手", new_sell_path, :class => "btn btn-info btn-large") %>
      <hr>
    </div>
    <div class="span12" align="center">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th width="50px">No.</th>
            <th width="80px">類別</th>
            <th class="text-center">名稱</th>
            <th width="100px">售價</th>
            <th width="100px">賣方</th>
            <th width="100px">發佈日期</th>
          </tr>
        </thead>
        <tbody>
          <% @lists.each_with_index do |list, index| %>
            <tr>
              <td><%= render_index_number(index, params[:page]) %></td>
              <td><%= render_category_name(list) %></td>
              <td class="text-center"><%# link_to(list.name, want_path(want), :class => "deal-title") %></td>
              <td><%= render_price(list) %></td>
              <td><%= render_user_name(list) %></td>
              <td><%= render_published_at(list) %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= will_paginate(@lists) %>
    </div>
  </div>
</div>